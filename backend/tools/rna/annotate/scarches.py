"""scArches reference mapping via pretrained model."""

from __future__ import annotations


def run(adata, *, refs):
    raise NotImplementedError("TODO: scArches reference mapping (Python, requires pretrained ref model in refs).")
